import styled from "styled-components/native";
import { ContainerButton } from "../ContainerLogin";

export const ButtonDelete = styled(ContainerButton)`
   border-color: ${({ theme }) => theme.colors.attention};

`